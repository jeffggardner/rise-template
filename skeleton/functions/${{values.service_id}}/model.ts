export interface ${{values.service_name}}Request {
  /**
   * Add fields with @TJS annotations here
   */

  /**
   * @TJS-pattern ^\d
   */
  example: string;
}

export interface ${{values.service_name}}Response {
  /**
   * Add fields with @TJS annotations here
   */

  /**
   * @TJS-pattern ^\d
   */
  example: string;
}
